schemaVersion: '0.3'
description: |
  ### Document name - ASR-EnableSNSDeliveryLogging

  ## What does this document do?
  Enables appropriate SNS Delivery Logging, creating roles if required for SNS.2


  ## Input Parameters
  * AutomationAssumeRole: (Required) The Amazon Resource Name (ARN) of the AWS Identity and Access Management (IAM) role that allows Systems Manager Automation to perform the actions on your behalf.
  * SNSTopicArn: (Required) The Resource ID
  * Region: (Required) Finding Region

  ## Output Parameters
  * Remediation.Output: STDOUT and messages from the remediation steps.

assumeRole: '{{ AutomationAssumeRole }}'
parameters:
  AutomationAssumeRole:
    type: String
    description: (Required) The ARN of the role that allows Automation to perform the actions on your behalf.
    allowedPattern: '^arn:(?:aws|aws-us-gov|aws-cn):iam::\d{12}:role/[\w+=,.@-]+$'
  SNSTopicArn:
    type: String
    description: The ARN of the resource
    allowedPattern: '(.*)$'
  Region:
    type: String
    description: The REGION of the resource
    allowedPattern: '^[a-z]{2}(?:-gov)?-[a-z]+-\d$'

outputs:
  - Remediation.Output

mainSteps:
  - name: Remediation
    action: 'aws:executeScript'
    outputs:
      - Name: Output
        Selector: $.Payload.response
        Type: StringMap
    inputs:
      InputPayload:
        ResourceId: '{{SNSTopicArn}}'
        Region: '{{Region}}'
      Runtime: python3.8
      Handler: runbook_handler
      Script: |-
        %%SCRIPT=EnableSNSDeliveryLogging.py%%
    isEnd: true
